{
  "contract_id": "p0-experiment-bounds",
  "decision_id": "P0-D03",
  "acceptance_id": "P0-A02",
  "task_id": "p0-p02-experiment-bounds",
  "frozen_at": "2026-09-15T00:00:00Z",
  "frozen_before_runtime_measurement": true,
  "review_state": "measurement-review-pending",
  "scope": "experimental safety and comparison protocol, not product qualification",
  "series": {
    "cold_boot_attempts": 10,
    "warmup_cycles_discarded": 5,
    "measured_boot_dispose_cycles": 30,
    "maximum_total_cycles_per_run": 45,
    "randomized_order": true,
    "abort_on_safety_breach": true
  },
  "settling": {
    "post_dispose_sample_offsets_ms": [0, 250, 1000, 2000, 5000],
    "maximum_settling_ms": 5000,
    "quiescence_rule": "all generation-owned counters reach zero and the final three heap/Worker samples show no positive slope beyond measurement resolution"
  },
  "noise": {
    "report": ["median", "p95", "minimum", "maximum", "median-absolute-deviation"],
    "environment_fields": ["browser-version", "cpu-logical-count", "cross-origin-isolated", "visibility-state", "power-mode", "toolchain-digests"],
    "rerun_trigger": "median-absolute-deviation exceeds 20 percent of the median or visibility/power state changes",
    "rerun_disposition": "retain noisy run, classify inconclusive, then run a newly identified series"
  },
  "safety_ceilings": [
    {"id": "run-wall-clock", "limit": 1800, "unit": "seconds", "owner": "test-harness", "breach_action": "abort series and preserve partial evidence"},
    {"id": "single-startup", "limit": 120, "unit": "seconds", "owner": "generation-supervisor", "breach_action": "terminate generation and classify startup-timeout"},
    {"id": "generation-workers", "limit": 64, "unit": "agents", "owner": "generation-supervisor", "breach_action": "refuse creation and terminate generation"},
    {"id": "shared-memory", "limit": 536870912, "unit": "bytes", "owner": "runtime-generation", "breach_action": "refuse instantiation"},
    {"id": "aggregate-artifacts", "limit": 268435456, "unit": "bytes", "owner": "loader", "breach_action": "reject before allocation"},
    {"id": "browser-host-rss-observation", "limit": 2147483648, "unit": "bytes", "owner": "test-harness", "breach_action": "terminate browser fixture and preserve partial evidence"}
  ],
  "semantic_comparison": {
    "oracle": "same OTP source, same BEAM digests, native bootstrap versus browser Wasm",
    "normalization_allowlist": ["pid rendering", "reference rendering", "monotonic timestamp origin", "scheduler interleaving explicitly declared by case"],
    "required_exact_fields": ["case-id", "event-kind", "payload", "exit-class", "mailbox-order", "link-monitor-result", "timer-causality"],
    "mismatch_disposition": "fail the case; do not widen normalization after observing the mismatch without a reviewed contract revision"
  },
  "change_control": "a change creates a new contract revision, records its rationale before another run, and never rewrites the contract attached to retained evidence"
}
